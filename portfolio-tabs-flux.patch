warning: in the working copy of 'src/data/projects.ts', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/index.css', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/theme/themes.css', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/transitions/wipes.ts', LF will be replaced by CRLF the next time Git touches it
diff --git a/src/data/projects.ts b/src/data/projects.ts
index 168660e..a684143 100644
--- a/src/data/projects.ts
+++ b/src/data/projects.ts
@@ -4,6 +4,7 @@ import twilightShot from '../assets/shots/twilightvotes.png'
 import vaultShot from '../assets/shots/vault.png'
 import solaceShot from '../assets/shots/solace.png'
 import karmaticShot from '../assets/shots/karmatic.png'
+import fluxShot from '../assets/shots/flux.png'
 
 export interface ProjectLink {
   label: string
@@ -153,4 +154,32 @@ export const projects: Project[] = [
     theme: 'karmatic',
     wipe: 'crt',
   },
+  {
+    slug: 'flux',
+    index: '06',
+    name: 'FLUX',
+    tag: 'Arcade',
+    year: '2026',
+    role: 'Front-end — physics, audio & shaders',
+    tagline: 'Throw, pluck, and splatter — a physics-and-sound arcade.',
+    description:
+      'FLUX is a kinetic arcade built with Svelte 5, Rapier2D and the Web Audio API. Drag orbs to fling them, watch collisions become notes and paint splats, toggle gravity and black-hole mode, cycle four foil palettes, and save your splatters as PNGs. Every interaction is immediate — there is no backend, no login, and no build-time API, just canvas, WASM physics and shaders.',
+    highlights: [
+      'Svelte 5 runes + SvelteKit static — the only non-React project in the catalog to prove stack diversity',
+      'Rapier2D-compat WASM physics: dynamic bodies, restitution 0.88, pinball platforms, pointer-driven impulses',
+      'Web Audio collider: collision distance check debounced 120 ms, triangle oscillator through lowpass, quantized to pentatonic/major/minor/chromatic',
+      'Canvas paint trails: collision splats with highlight dots, translucent fade, toDataURL snapshots persisted in localStorage',
+      'Fake fluid shader: three drifting radial gradients with globalCompositeOperation lighter + grain for a breathing foil field',
+      'Six+ input modes: drag/fling, click-to-spawn, shake (Space), gravity invert (G), black hole (B), palette (1-4), paint/sound toggles, Konami code, tilt on mobile',
+    ],
+    tech: ['Svelte 5', 'SvelteKit', 'Rapier', 'Web Audio API', 'Canvas', 'Vite'],
+    links: [
+      { label: 'Visit live site', href: 'https://cycoconutz.github.io/flux/', kind: 'live' },
+      { label: 'View source', href: 'https://github.com/cycoconutz/flux', kind: 'repo' },
+    ],
+    shot: fluxShot,
+    theme: 'flux',
+    wipe: 'shatter',
+  },
+
 ]
diff --git a/src/index.css b/src/index.css
index a458314..1fcc5e0 100644
--- a/src/index.css
+++ b/src/index.css
@@ -630,6 +630,33 @@ button {
   );
 }
 
+
+[data-project='flux'] .flourish {
+  opacity: 0.52;
+  background:
+    radial-gradient(38rem 38rem at 18% 22%, color-mix(in srgb, var(--accent) 42%, transparent), transparent 68%),
+    radial-gradient(34rem 34rem at 84% 78%, color-mix(in srgb, var(--accent-2) 38%, transparent), transparent 68%),
+    radial-gradient(28rem 28rem at 55% 12%, rgba(255,255,255,0.08), transparent 62%);
+  filter: blur(0.5px);
+}
+
+[data-theme='flux'] .project-name {
+  font-family: 'Syne', 'Space Grotesk', sans-serif;
+  font-weight: 800;
+  letter-spacing: -0.04em;
+  background: linear-gradient(180deg, #fff 12%, var(--fg) 56%, var(--accent) 100%);
+  -webkit-background-clip: text;
+  background-clip: text;
+  color: transparent;
+  filter: drop-shadow(0 8px 22px color-mix(in srgb, var(--accent) 22%, transparent));
+}
+
+[data-theme='flux'] .tile-name,
+[data-theme='flux'] .spine-name {
+  font-family: 'Syne', sans-serif;
+  font-weight: 800;
+}
+
 /* theme-specific signatures */
 [data-theme='twilight'] .project-name {
   font-size: clamp(2.2rem, 7vw, 5rem);
@@ -652,6 +679,45 @@ button {
   text-shadow: 0 0 16px rgba(57, 255, 136, 0.6);
 }
 
+
+/* shatter */
+.curtain[data-wipe='shatter'] .curtain-panel {
+  display: block;
+  background: var(--wipe-bg);
+  clip-path: polygon(50% 50%, 50% 50%, 50% 50%);
+}
+.curtain[data-wipe='shatter'] .curtain-panel:nth-child(1) { background: var(--wipe-bg); }
+.curtain[data-wipe='shatter'] .curtain-panel:nth-child(2) { background: var(--wipe-bg-2); }
+.curtain[data-wipe='shatter'] .curtain-panel:nth-child(3) { background: var(--wipe-bg); }
+.curtain[data-wipe='shatter'][data-phase='cover'] .curtain-panel {
+  animation: shatterIn 0.58s var(--ease) both;
+  animation-delay: calc(var(--i) * 52ms);
+}
+.curtain[data-wipe='shatter'][data-phase='reveal'] .curtain-panel {
+  animation: shatterOut 0.52s var(--ease) both;
+  animation-delay: calc((5 - var(--i)) * 42ms);
+}
+.curtain[data-wipe='shatter'] .curtain-pattern {
+  display: block;
+  background-image:
+    linear-gradient(45deg, rgba(255,255,255,0.14) 1px, transparent 1px),
+    linear-gradient(-45deg, rgba(255,255,255,0.10) 1px, transparent 1px);
+  background-size: 28px 28px;
+  opacity: 0;
+}
+.curtain[data-wipe='shatter'][data-phase='cover'] .curtain-pattern {
+  animation: patternIn 0.28s 0.32s var(--ease) both;
+}
+@keyframes shatterIn {
+  0% { clip-path: polygon(50% 50%, 50% 50%, 50% 50%, 50% 50%); transform: scale(0.6) rotate(-6deg); opacity: 0; }
+  60% { opacity: 1; }
+  100% { clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%); transform: scale(1) rotate(0deg); opacity: 1; }
+}
+@keyframes shatterOut {
+  0% { clip-path: polygon(0 0, 100% 0, 100% 100%, 0 100%); transform: scale(1) rotate(0deg); opacity: 1; }
+  100% { clip-path: polygon(50% 0, 100% 50%, 50% 100%, 0 50%); transform: scale(1.18) rotate(3deg); opacity: 0; }
+}
+
 /* -------------------------------------------------------------- curtain --- */
 
 .curtain {
diff --git a/src/theme/themes.css b/src/theme/themes.css
index 7ab56cb..e25e90f 100644
--- a/src/theme/themes.css
+++ b/src/theme/themes.css
@@ -102,3 +102,20 @@
   --display-transform: uppercase;
   --shadow: 0 0 44px -8px rgba(57, 255, 136, 0.55);
 }
+
+[data-theme='flux'] {
+  --bg: #07090f;
+  --bg-2: #131528;
+  --fg: #f2f5ff;
+  --muted: #9aa0c2;
+  --accent: #beff3b;
+  --accent-2: #ff3beb;
+  --border: rgba(255,255,255,0.09);
+  --card: #121426;
+  --radius: 18px;
+  --font-display: 'Syne', 'Space Grotesk', sans-serif;
+  --font-body: 'Space Grotesk', system-ui, sans-serif;
+  --font-mono: 'JetBrains Mono', monospace;
+  --display-transform: none;
+  --shadow: 0 26px 70px -24px rgba(190,255,59,0.35), 0 0 40px -16px rgba(255,59,235,0.28);
+}
diff --git a/src/transitions/wipes.ts b/src/transitions/wipes.ts
index 81ee747..0c34d1f 100644
--- a/src/transitions/wipes.ts
+++ b/src/transitions/wipes.ts
@@ -1,6 +1,6 @@
-export type WipeId = 'bloom' | 'scanline' | 'riso' | 'agenda' | 'crt'
+export type WipeId = 'bloom' | 'scanline' | 'riso' | 'agenda' | 'crt' | 'shatter'
 
-export const wipes: WipeId[] = ['bloom', 'scanline', 'riso', 'agenda', 'crt']
+export const wipes: WipeId[] = ['bloom', 'scanline', 'riso', 'agenda', 'crt', 'shatter']
 
 export const wipeNames: Record<WipeId, string> = {
   bloom: 'Radial bloom',
@@ -8,4 +8,5 @@ export const wipeNames: Record<WipeId, string> = {
   riso: 'Risograph press',
   agenda: 'Agenda cards',
   crt: 'CRT collapse',
+  shatter: 'Prism shatter',
 }
